#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * 
 *
 */
int main(viod)
{
        int n;
	int last_n
	srand(time(0));
        n = rand() - RAND_MAX / 2;
	/* your code goes there */
	
	last_n = n % 10;
	if (last_n > 5)
                printf("last digit of %d is %d and is greater than 5\n", n, Last_n);
	else if (last_n == 0)
		printf(
	return (0)

}
